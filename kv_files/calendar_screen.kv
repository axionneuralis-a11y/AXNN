<CalendarScreen>:
    BoxLayout:
        orientation: 'vertical'
        MDToolbar:
            title: "Calendar"
            right_action_items: [['console', lambda x: root.open_editor()]]
        # Widget kalender bulanan (M2 — calendar_widget.py) 

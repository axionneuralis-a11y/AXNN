<NotesScreen>:
    BoxLayout:
        orientation: 'vertical'
        MDToolbar:
            title: "Notes"
            right_action_items: [['console', lambda x: root.open_editor()]]
        ScrollView:
            MDList:
                # Render notes list via RecycleView (M2) 

<TodosScreen>:
    BoxLayout:
        orientation: 'vertical'
        MDToolbar:
            title: "List"
            right_action_items: [['console', lambda x: root.open_editor()]]
        ScrollView:
            MDList:
                # Render todo items via RecycleView (M2) 
